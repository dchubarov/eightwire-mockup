import React from 'react';
import {Box, Typography} from "@mui/material";

const App: React.FC = () => {
    return (
        <Box>
            <Typography variant="h3">App</Typography>
        </Box>
    );
}

export default App;
